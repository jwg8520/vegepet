{
  "lineNo": 768,
  "callIndex": 11,
  "name": "StrReplace",
  "path": "d:\\CURSOR_VegePet\\vegepet\\lib\\main.dart",
  "old_string": "  String? _stageGrowthMessage(String? beforeStage, String afterStage) {\n    if (beforeStage == null || beforeStage == afterStage) return null;\n    final l10n = AppLocalizations.of(context);\n    switch (afterStage) {\n      case 'child':\n        return l10n.snackStageGrewToChild;\n      case 'grown':\n        return l10n.snackStageGrewToGrown;\n      case 'adult':\n        return l10n.snackStageGrewToAdult;\n      default:\n        return null;\n    }\n  }\n\n  /// affection 변경 후 _activePet 을 다시 조회한 상태에서 호출한다.\n  /// - DB 의 stage 가 affection 기준과 다르면 stage 컬럼을 맞춰준다.\n  /// - 단계 변화가 있으면 SnackBar 로 안내.\n  /// - 최초 adult 도달 시 [_handleAdultGraduationIfNeeded] 로 육성 완료 처리.\n  Future<void> _syncStageAfterAffectionChange({\n    required String? beforeStage,\n  }) async {\n    if (_activePet == null) return;\n    final affection = (_activePet!['affection'] as num?)?.toInt() ?? 0;\n    final dbStage = _activePet!['stage']?.toString() ?? 'baby';\n    final targetStage = _stageFromAffection(affection);\n\n    if (dbStage != targetStage) {\n      try {\n        await supabase\n            .from('user_pets')\n            .update({'stage': targetStage})\n            .eq('id', _activePet!['id']);\n        await _fetchActivePet(syncYard: false);\n        if (mounted) setState(() {});\n      } catch (e) {\n        debugPrint('stage sync failed: $e');\n      }\n    }\n\n    // adult 도달은 _handleAdultGraduationIfNeeded 안에서 별도 스낵바를 띄우므로\n    // 여기서는 child/grown 전환만 안내한다.\n    if (targetStage != 'adult') {\n      final message = _stageGrowthMessage(beforeStage, targetStage);\n      if (message != null && mounted) _showSnack(message);\n    }\n\n    await _handleAdultGraduationIfNeeded(beforeStage: beforeStage);\n  }",
  "new_string": "  String? _stageGrowthMessage(String? beforeStage, String afterStage) {\n    if (beforeStage == null || beforeStage == afterStage) return null;\n    final l10n = AppLocalizations.of(context);\n    final name = _activePetDisplayNickname();\n    switch (afterStage) {\n      case 'child':\n        return name == null\n            ? l10n.interactionGrowthYoungFallback\n            : l10n.interactionGrowthYoung(name);\n      case 'grown':\n        return name == null\n            ? l10n.interactionGrowthGrownFallback\n            : l10n.interactionGrowthGrown(name);\n      case 'adult':\n        return name == null\n            ? l10n.interactionGrowthAdultFallback\n            : l10n.interactionGrowthAdult(name);\n      default:\n        return null;\n    }\n  }\n\n  /// affection 변경 후 _activePet 을 다시 조회한 상태에서 호출한다.\n  /// - DB 의 stage 가 affection 기준과 다르면 stage 컬럼을 맞춰준다.\n  /// - 단계 변화가 있으면 교감 상태 메시지로 안내.\n  /// - 최초 adult 도달 시 [_handleAdultGraduationIfNeeded] 로 육성 완료 처리.\n  Future<void> _syncStageAfterAffectionChange({\n    required String? beforeStage,\n  }) async {\n    if (_activePet == null) return;\n    final affection = (_activePet!['affection'] as num?)?.toInt() ?? 0;\n    final dbStage = _activePet!['stage']?.toString() ?? 'baby';\n    final targetStage = _stageFromAffection(affection);\n\n    if (dbStage != targetStage) {\n      try {\n        await supabase\n            .from('user_pets')\n            .update({'stage': targetStage})\n            .eq('id', _activePet!['id']);\n        await _fetchActivePet(syncYard: false);\n        if (mounted) setState(() {});\n      } catch (e) {\n        debugPrint('stage sync failed: $e');\n      }\n    }\n\n    final petId = _activePet?['id']?.toString() ?? '';\n    final growthEventKey = (beforeStage != null && beforeStage != targetStage)\n        ? 'growth:$petId:$beforeStage:$targetStage'\n        : null;\n    final growthMessage = _stageGrowthMessage(beforeStage, targetStage);\n\n    // adult 도달은 성숙기 축하 modal 종료 후 교감 메시지로 안내한다.\n    // 여기서는 child/grown 전환만 즉시(또는 queue) 표시.\n    if (targetStage != 'adult' && growthMessage != null) {\n      await _showInteractionStatusMessage(\n        growthMessage,\n        eventKey: growthEventKey,\n      );\n    }\n\n    await _handleAdultGraduationIfNeeded(beforeStage: beforeStage);\n\n    if (targetStage == 'adult' && growthMessage != null) {\n      await _showInteractionStatusMessage(\n        growthMessage,\n        eventKey: growthEventKey,\n      );\n    }\n  }",
  "replace_all": null,
  "contents": null
}