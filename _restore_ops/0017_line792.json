{
  "lineNo": 792,
  "callIndex": 17,
  "name": "StrReplace",
  "path": "d:\\CURSOR_VegePet\\vegepet\\lib\\main.dart",
  "old_string": "String _localizedMealFeedbackPhrase(String code, AppLocalizations l10n) {\n  switch (code) {\n    case 'protein_up':\n      return l10n.mealFeedbackProteinUp;\n    case 'protein_down':\n      return l10n.mealFeedbackProteinDown;\n    case 'carbohydrates_up':\n      return l10n.mealFeedbackCarbohydratesUp;\n    case 'carbohydrates_down':\n      return l10n.mealFeedbackCarbohydratesDown;\n    case 'fat_up':\n      return l10n.mealFeedbackFatUp;\n    case 'fat_down':\n      return l10n.mealFeedbackFatDown;\n    case 'dietary_fiber_up':\n      return l10n.mealFeedbackDietaryFiberUp;\n    case 'dietary_fiber_down':\n      return l10n.mealFeedbackDietaryFiberDown;\n    default:\n      return '';\n  }\n}\n\nString _joinFeedbackPhrases(List<String> phrases, {required bool isEnglish}) {\n  final parts = phrases.where((p) => p.trim().isNotEmpty).toList();\n  if (parts.isEmpty) return '';\n  if (parts.length == 1) return parts.first;\n  if (!isEnglish) {\n    return parts.join(', ');\n  }\n  if (parts.length == 2) {\n    return '${parts[0]} and ${parts[1]}';\n  }\n  return '${parts.sublist(0, parts.length - 1).join(', ')}, and ${parts.last}';\n}\n\nString? _localizedMealFeedbackFromCodes(\n  List<String> codes,\n  AppLocalizations l10n, {\n  required bool isEnglish,\n}) {\n  final phrases = codes\n      .map((c) => _localizedMealFeedbackPhrase(c, l10n))\n      .where((p) => p.isNotEmpty)\n      .toList();\n  final joined = _joinFeedbackPhrases(phrases, isEnglish: isEnglish);\n  return joined.isEmpty ? null : joined;\n}\n\nbool _isDisallowedFallbackFeedback(String text, {required bool isEnglish}) {\n  final lower = text.toLowerCase();\n  final banned = isEnglish\n      ? _kDisallowedFeedbackFallbackEn\n      : _kDisallowedFeedbackFallbackKo;\n  for (final word in banned) {\n    if (isEnglish) {\n      if (lower.contains(word.toLowerCase())) return true;\n    } else if (text.contains(word)) {\n      return true;\n    }\n  }\n  return false;\n}\n\n/// English locale: comma-separated feedback phrases → natural \"and\" list.\nString _formatFeedbackForEnglishSentence(String feedback) {\n  final cleaned = feedback\n      .replaceAll(RegExp(r'\\s+'), ' ')\n      .trim();\n  if (cleaned.isEmpty) return cleaned;\n  final parts = cleaned\n      .split(RegExp(r'\\s*,\\s*|\\s+and\\s+', caseSensitive: false))\n      .map((part) => part.trim())\n      .where((part) => part.isNotEmpty)\n      .toList();\n\n  if (parts.length <= 1) return cleaned;\n  if (parts.length == 2) return '${parts[0]} and ${parts[1]}';\n  return '${parts.sublist(0, parts.length - 1).join(', ')}, and ${parts.last}';\n}\n\nString _pickMealTemplate(List<String> templates) {\n  if (templates.isEmpty) return '';\n  return templates[_mealMessageRandom.nextInt(templates.length)];\n}\n\n/// AI 판정 결과 + feedback_codes/feedback_text → 앱 표시용 감성 메시지.\nString _buildAiStatusMessage({\n  required String? resultType,\n  required List<String> feedbackCodes,\n  required String? feedbackText,\n  required AppLocalizations l10n,\n  required bool isEnglish,\n}) {\n  switch (resultType) {\n    case 'perfect':\n      return _pickMealTemplate([\n        l10n.mealPerfectMessage1,\n        l10n.mealPerfectMessage2,\n      ]);\n    case 'good':\n      {\n        final fromCodes = _localizedMealFeedbackFromCodes(\n          feedbackCodes,\n          l10n,\n          isEnglish: isEnglish,\n        );\n        var feedback = fromCodes;\n        if (feedback == null || feedback.isEmpty) {\n          final raw = feedbackText?.trim() ?? '';\n          if (raw.isNotEmpty &&\n              !_isDisallowedFallbackFeedback(raw, isEnglish: isEnglish)) {\n            feedback = isEnglish ? _formatFeedbackForEnglishSentence(raw) : raw;\n          }\n        }\n        if (feedback != null && feedback.isNotEmpty) {\n          return _pickMealTemplate([\n            l10n.mealGoodFeedbackMessage1(feedback),\n            l10n.mealGoodFeedbackMessage2(feedback),\n          ]);\n        }\n        return l10n.mealGoodFallbackMessage;\n      }\n    case 'bad':\n      {\n        final fromCodes = _localizedMealFeedbackFromCodes(\n          feedbackCodes,\n          l10n,\n          isEnglish: isEnglish,\n        );\n        var feedback = fromCodes;\n        if (feedback == null || feedback.isEmpty) {\n          final raw = feedbackText?.trim() ?? '';\n          if (raw.isNotEmpty &&\n              !_isDisallowedFallbackFeedback(raw, isEnglish: isEnglish)) {\n            feedback = isEnglish ? _formatFeedbackForEnglishSentence(raw) : raw;\n          }\n        }\n        if (feedback != null && feedback.isNotEmpty) {\n          return _pickMealTemplate([\n            l10n.mealBadFeedbackMessage1(feedback),\n            l10n.mealBadFeedbackMessage2(feedback),\n          ]);\n        }\n        return l10n.mealBadFallbackMessage;\n      }\n    case 'uncertain':\n    default:\n      return l10n.mealRecognitionRetryMessage;\n  }\n}",
  "new_string": "String _localizedMealFeedbackPhrase(String code, AppLocalizations l10n) {\n  switch (code) {\n    case 'protein_up':\n      return l10n.mealFeedbackProteinUp;\n    case 'protein_down':\n      return l10n.mealFeedbackProteinDown;\n    case 'carbohydrates_up':\n      return l10n.mealFeedbackCarbohydratesUp;\n    case 'carbohydrates_down':\n      return l10n.mealFeedbackCarbohydratesDown;\n    case 'fat_up':\n      return l10n.mealFeedbackFatUp;\n    case 'fat_down':\n      return l10n.mealFeedbackFatDown;\n    case 'dietary_fiber_up':\n      return l10n.mealFeedbackDietaryFiberUp;\n    case 'dietary_fiber_down':\n      return l10n.mealFeedbackDietaryFiberDown;\n    default:\n      return '';\n  }\n}\n\n/// 영어 교감 문장 삽입용 소문자 동사구 (let’s {feedback}).\nString _localizedMealFeedbackActionPhrase(String code) {\n  switch (code) {\n    case 'protein_up':\n      return 'increase protein';\n    case 'protein_down':\n      return 'reduce protein';\n    case 'carbohydrates_up':\n      return 'increase carbohydrates';\n    case 'carbohydrates_down':\n      return 'reduce carbohydrates';\n    case 'fat_up':\n      return 'increase fat';\n    case 'fat_down':\n      return 'reduce fat';\n    case 'dietary_fiber_up':\n      return 'increase dietary fiber';\n    case 'dietary_fiber_down':\n      return 'reduce dietary fiber';\n    default:\n      return '';\n  }\n}\n\nString _joinFeedbackPhrases(List<String> phrases, {required bool isEnglish}) {\n  final parts = phrases.where((p) => p.trim().isNotEmpty).toList();\n  if (parts.isEmpty) return '';\n  if (parts.length == 1) return parts.first;\n  if (!isEnglish) {\n    return parts.join(', ');\n  }\n  if (parts.length == 2) {\n    return '${parts[0]} and ${parts[1]}';\n  }\n  return '${parts.sublist(0, parts.length - 1).join(', ')}, and ${parts.last}';\n}\n\nString? _localizedMealFeedbackFromCodes(\n  List<String> codes,\n  AppLocalizations l10n, {\n  required bool isEnglish,\n}) {\n  final phrases = codes\n      .map((c) => _localizedMealFeedbackPhrase(c, l10n))\n      .where((p) => p.isNotEmpty)\n      .toList();\n  final joined = _joinFeedbackPhrases(phrases, isEnglish: isEnglish);\n  return joined.isEmpty ? null : joined;\n}\n\nString? _localizedMealFeedbackForInteraction(\n  List<String> codes,\n  AppLocalizations l10n, {\n  required bool isEnglish,\n}) {\n  if (isEnglish) {\n    final phrases = codes\n        .map(_localizedMealFeedbackActionPhrase)\n        .where((p) => p.isNotEmpty)\n        .toList();\n    final joined = _joinFeedbackPhrases(phrases, isEnglish: true);\n    return joined.isEmpty ? null : joined;\n  }\n  return _localizedMealFeedbackFromCodes(\n    codes,\n    l10n,\n    isEnglish: false,\n  );\n}\n\nString _lowercaseFirstAsciiLetter(String text) {\n  final trimmed = text.trim();\n  if (trimmed.isEmpty) return trimmed;\n  final first = trimmed[0];\n  if (first.codeUnitAt(0) >= 65 && first.codeUnitAt(0) <= 90) {\n    return '${first.toLowerCase()}${trimmed.substring(1)}';\n  }\n  return trimmed;\n}\n\nbool _isDisallowedFallbackFeedback(String text, {required bool isEnglish}) {\n  final lower = text.toLowerCase();\n  final banned = isEnglish\n      ? _kDisallowedFeedbackFallbackEn\n      : _kDisallowedFeedbackFallbackKo;\n  for (final word in banned) {\n    if (isEnglish) {\n      if (lower.contains(word.toLowerCase())) return true;\n    } else if (text.contains(word)) {\n      return true;\n    }\n  }\n  return false;\n}\n\n/// English locale: comma-separated feedback phrases → natural \"and\" list.\nString _formatFeedbackForEnglishSentence(String feedback) {\n  final cleaned = feedback\n      .replaceAll(RegExp(r'\\s+'), ' ')\n      .trim();\n  if (cleaned.isEmpty) return cleaned;\n  final parts = cleaned\n      .split(RegExp(r'\\s*,\\s*|\\s+and\\s+', caseSensitive: false))\n      .map((part) => part.trim())\n      .where((part) => part.isNotEmpty)\n      .map(_lowercaseFirstAsciiLetter)\n      .toList();\n\n  if (parts.length <= 1) {\n    return parts.isEmpty ? cleaned : parts.first;\n  }\n  if (parts.length == 2) return '${parts[0]} and ${parts[1]}';\n  return '${parts.sublist(0, parts.length - 1).join(', ')}, and ${parts.last}';\n}\n\nbool _isHangulSyllableCodeUnit(int codeUnit) {\n  return codeUnit >= 0xAC00 && codeUnit <= 0xD7A3;\n}\n\nbool _hasKoreanFinalConsonant(String text) {\n  final trimmed = text.trim();\n  if (trimmed.isEmpty) return false;\n  final codeUnit = trimmed.runes.last;\n  if (!_isHangulSyllableCodeUnit(codeUnit)) return false;\n  return (codeUnit - 0xAC00) % 28 != 0;\n}\n\n/// 이름 + 이/가 (비한글 마지막 글자는 `가` fallback).\nString _petNameWithSubjectParticle(String name) {\n  final trimmed = name.trim();\n  if (trimmed.isEmpty) return trimmed;\n  final codeUnit = trimmed.runes.last;\n  if (!_isHangulSyllableCodeUnit(codeUnit)) return '$trimmed가';\n  return _hasKoreanFinalConsonant(trimmed) ? '$trimmed이' : '$trimmed가';\n}\n\n/// 이름 + 은/는 (비한글 마지막 글자는 `는` fallback).\nString _petNameWithTopicParticle(String name) {\n  final trimmed = name.trim();\n  if (trimmed.isEmpty) return trimmed;\n  final codeUnit = trimmed.runes.last;\n  if (!_isHangulSyllableCodeUnit(codeUnit)) return '$trimmed는';\n  return _hasKoreanFinalConsonant(trimmed) ? '$trimmed은' : '$trimmed는';\n}\n\nString _pickInteractionTemplate(List<String> templates) {\n  if (templates.isEmpty) return '';\n  return templates[_mealMessageRandom.nextInt(templates.length)];\n}\n\n/// AI 판정 결과 + feedback_codes/feedback_text → 앱 표시용 감성 메시지.\nString _buildAiStatusMessage({\n  required String? resultType,\n  required List<String> feedbackCodes,\n  required String? feedbackText,\n  required AppLocalizations l10n,\n  required bool isEnglish,\n  required String petName,\n  required int affectionGain,\n}) {\n  final petNameSubject = isEnglish\n      ? petName\n      : _petNameWithSubjectParticle(petName);\n  final petNameTopic = isEnglish\n      ? petName\n      : _petNameWithTopicParticle(petName);\n\n  switch (resultType) {\n    case 'perfect':\n      return _pickInteractionTemplate([\n        l10n.mealPerfectMessage1(petNameSubject, affectionGain),\n        l10n.mealPerfectMessage2(petNameTopic, affectionGain),\n      ]);\n    case 'good':\n      {\n        final fromCodes = _localizedMealFeedbackForInteraction(\n          feedbackCodes,\n          l10n,\n          isEnglish: isEnglish,\n        );\n        var feedback = fromCodes;\n        if (feedback == null || feedback.isEmpty) {\n          final raw = feedbackText?.trim() ?? '';\n          if (raw.isNotEmpty &&\n              !_isDisallowedFallbackFeedback(raw, isEnglish: isEnglish)) {\n            feedback = isEnglish\n                ? _formatFeedbackForEnglishSentence(raw)\n                : raw;\n          }\n        }\n        if (feedback != null && feedback.isNotEmpty) {\n          return _pickInteractionTemplate([\n            l10n.mealGoodFeedbackMessage1(\n              petNameSubject,\n              feedback,\n              affectionGain,\n            ),\n            l10n.mealGoodFeedbackMessage2(petName, feedback, affectionGain),\n          ]);\n        }\n        return l10n.mealGoodFallbackMessage(petNameSubject);\n      }\n    case 'bad':\n      {\n        final fromCodes = _localizedMealFeedbackForInteraction(\n          feedbackCodes,\n          l10n,\n          isEnglish: isEnglish,\n        );\n        var feedback = fromCodes;\n        if (feedback == null || feedback.isEmpty) {\n          final raw = feedbackText?.trim() ?? '';\n          if (raw.isNotEmpty &&\n              !_isDisallowedFallbackFeedback(raw, isEnglish: isEnglish)) {\n            feedback = isEnglish\n                ? _formatFeedbackForEnglishSentence(raw)\n                : raw;\n          }\n        }\n        if (feedback != null && feedback.isNotEmpty) {\n          return _pickInteractionTemplate([\n            l10n.mealBadFeedbackMessage1(petName, feedback, affectionGain),\n            l10n.mealBadFeedbackMessage2(\n              petNameTopic,\n              feedback,\n              affectionGain,\n            ),\n          ]);\n        }\n        return l10n.mealBadFallbackMessage(petName);\n      }\n    case 'uncertain':\n    default:\n      return l10n.mealRecognitionRetryMessage;\n  }\n}",
  "replace_all": null,
  "contents": null
}