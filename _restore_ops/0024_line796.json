{
  "lineNo": 796,
  "callIndex": 24,
  "name": "StrReplace",
  "path": "d:\\CURSOR_VegePet\\vegepet\\lib\\main.dart",
  "old_string": "    setState(() => _isInteracting = true);\n\n    try {\n      final petId = _activePet!['id'];\n      final currentAffection = (_activePet!['affection'] as num?)?.toInt() ?? 0;\n      final beforeStage = _activePet!['stage']?.toString() ?? 'baby';\n\n      final nextAffection = currentAffection + 1;\n      final nextStage = _stageFromAffection(nextAffection);\n\n      await supabase\n          .from('user_pets')\n          .update({\n            'affection': nextAffection,\n            'stage': nextStage,\n            dateColumn: today,\n          })\n          .eq('id', petId);\n\n      await _fetchActivePet();\n\n      if (!mounted) return;\n      setState(() => _isInteracting = false);\n\n      final eventPetId = petId.toString();\n      if (isPlay) {\n        await _showInteractionStatusMessage(\n          _interactionPlaySuccessMessage(),\n          eventKey: 'play:$eventPetId:$today',\n        );\n      } else {\n        await _showInteractionStatusMessage(\n          _interactionPettingSuccessMessage(),\n          eventKey: 'pet:$eventPetId:$today',\n        );\n      }\n\n      await _syncStageAfterAffectionChange(beforeStage: beforeStage);",
  "new_string": "    setState(() => _isInteracting = true);\n\n    try {\n      final petId = _activePet!['id'];\n      final currentAffection = (_activePet!['affection'] as num?)?.toInt() ?? 0;\n      final beforeStage = _activePet!['stage']?.toString() ?? 'baby';\n\n      final affectionGain = 1;\n      final nextAffection = currentAffection + affectionGain;\n      final nextStage = _stageFromAffection(nextAffection);\n\n      await supabase\n          .from('user_pets')\n          .update({\n            'affection': nextAffection,\n            'stage': nextStage,\n            dateColumn: today,\n          })\n          .eq('id', petId);\n\n      await _fetchActivePet();\n\n      if (!mounted) return;\n      setState(() => _isInteracting = false);\n\n      final eventPetId = petId.toString();\n      if (isPlay) {\n        await _showInteractionStatusMessage(\n          _interactionPlaySuccessMessage(affectionGain: affectionGain),\n          eventKey: 'play_success:$today:$eventPetId',\n        );\n      } else {\n        await _showInteractionStatusMessage(\n          _interactionPettingSuccessMessage(affectionGain: affectionGain),\n          eventKey: 'petting:$today:$eventPetId',\n        );\n      }\n\n      await _syncStageAfterAffectionChange(beforeStage: beforeStage);",
  "replace_all": null,
  "contents": null
}